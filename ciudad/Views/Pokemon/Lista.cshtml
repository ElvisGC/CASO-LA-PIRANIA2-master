@model List<Pokemon>

<h1>Lista de Usuarios</h1>

@if (Model.Count == 0) {
    <div class="alert alert-warning text-center">Aún no hay usuarios registrados.</div>
} else {
    <ol>
    @foreach (var p in Model) {
        <li class="d-flex">
            <div>@p.Nombre</div><br>
         </li>   
            <div>@p.Correo</div>
       
    }
    </ol>
}